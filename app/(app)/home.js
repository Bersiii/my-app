import { StatusBar } from "expo-status-bar";


// Fixed folder path typo ('contextt' -> 'context')

const Home = () => {
  return (
    <>
      <StatusBar style="dark" />
    </>
  );
};

export default Home;
